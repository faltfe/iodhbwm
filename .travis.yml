
branches:
  only:
    - master
    - develop
install:
 - source ./support/texlive_install.sh
cache:
  directories:
    - /tmp/texlive
    - $HOME/.texlive
script:
# Prefix command with travis_wait x so it times out after 3 mins
# Run basic example
- travis_wait 3 pdflatex -interaction=nonstopmode -halt-on-error ./examples/basic-usage/iodhbwm-example
- travis_wait 3 biblatex ./examples/basic-usage/iodhbwm-example
- travis_wait 3 pdflatex -interaction=nonstopmode -halt-on-error ./examples/basic-usage/iodhbwm-example
